# npm install output
node_modules/

# TypeScript compiler output
out/

# Language-server payloads staged by Build.ps1. These are ~18 MB per runtime identifier and are
# reproduced from tooling/Assimalign.Viu.LanguageServer; they are never committed.
server/

# vsce package output
*.vsix
