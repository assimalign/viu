<template>
<article>{{ RecordRender() }}<slot /></article>
</template>

@script {
public int RenderCount { get; private set; }

private string RecordRender() => (RenderCount++, string.Empty).Item2;
}
