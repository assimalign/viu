namespace Assimalign.Viu.DevTools;

/// <summary>Names the standalone runtime-inspection JSON protocol and its current version.</summary>
public static class DevToolsProtocol
{
    /// <summary>The stable protocol name written into every batch.</summary>
    public const string Name = "assimalign.viu.devtools";

    /// <summary>The newest protocol version implemented by this package.</summary>
    public const int CurrentVersion = 1;
}
