# Viu branding artifacts

These files are imported from the read-only `C:\Source\repos\assimalign\branding` repository. Regenerate branding there and copy only referenced Viu artifacts here; do not edit the imported images in this repository. The Visual Studio Code package directories carry byte-identical local copies because `vsce` cannot package files outside an extension root.

The colored dark NuGet tile is the branding source's default and keeps a strong light glyph on dark surfaces while its opaque tile separates clearly from light surfaces. Transparent `on-light` and `on-dark` marks are selected by editor theme where supported; single-image marketplace placements use the `on-light` mark because its dark skeleton remains legible on the common light listing surface.
